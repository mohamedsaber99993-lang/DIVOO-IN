//import db from '../lib/database.js'

let handler = async (m, { conn, isOwner, isAdmin, isROwner} ) => {
    
    // 1. قفل الحماية: التحقق مما إذا كان مرسل الأمر مشرفاً أو مطوراً، وإلا يتم رفض الطلب تلقائياً
    if (!(isAdmin || isOwner)) return dfail('admin', m, conn)
    
    // 2. تحديث قاعدة بيانات الجروب وتحويل حالة الحظر إلى (ملغية) لإعادة تنشيط البوت
    global.db.data.chats[m.chat].isBanned = false
    
    // 3. إرسال رسالة التأكيد والترحيب باللغة العربية
    m.reply(`✅ تم إلغاء الحظر، البوت الآن نشط وجاهز للعمل في هذه المجموعة.`)   
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها لسهولة الوصول من المنيو العربي
handler.help = ['تنشيط_البوت']
handler.tags = ['أوامر المطور والمشرف']

// mصفوفة الكلمات المفتاحية والاختصارات للأمر باللغتين العربية والإنجليزية
handler.command = ['تنشيط_البوت', 'تشغيل_البوت', 'تفعيل_البوت', 'chaton', 'unbanchat'] 

export default handler
