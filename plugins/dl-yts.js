import yts from 'yt-search'

let handler = async (m, { conn, text }) => {
  // 1. التحقق من إدخال نص البحث: إذا لم يكتب المستخدم شيئاً يتم تنبيهه برمجياً
  if (!text) throw `✳️ يرجى كتابة ما تريد البحث عنه في يوتيوب (YouTube).\nمثال: .yts سورة الكهف`
  
  // 2. تشغيل محرك البحث وجلب مصفوفة الفيديوهات الناتجة
  let results = await yts(text)
  let tes = results.videos

  // التحقق مما إذا كانت نتائج البحث فارغة لتجنب الأخطاء البرمجية
  if (!tes || tes.length === 0) throw '❌ لم يتم العثور على أي نتائج مطابقة لبحثك في يوتيوب.'

  // 3. استخدام دالة الخريطة (.map) للمرور على قائمة الفيديوهات وصياغتها باللغة العربية
  let teks = tes.map(v => `
📌 *العنوان:* ${v.title}
*⌚ المدة الزمنية:* ${v.timestamp}
*📆 تاريخ الرفع:* ${v.ago}
*👀 المشاهدات:* ${v.views.toLocaleString()}
*🔗 رابط المقطع:* ${v.url}
`.trim()).join('\n________________________\n\n')

  // 4. إرسال صورة غلاف النتيجة الأولى (Thumbnail) مدمجاً معها النص المنسق لكامل نتائج البحث
  await conn.sendFile(m.chat, tes[0].image, 'yts.jpeg', teks, m, null, fwc)
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها لسهولة الوصول من المنيو العربي
handler.help = ['بحث_يوتيوب'] 
handler.tags = ['التحري / الأدوات']

// مصفوفة الاختصارات والكلمات المفتاحية للأمر باللغتين العربية والإنجليزية
handler.command = ['بحث_يوتيوب', 'يوتيوب_بحث', 'ytsearch', 'yts'] 

export default handler
