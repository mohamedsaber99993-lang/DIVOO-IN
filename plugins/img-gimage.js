import fg from 'fg-senna';

let handler = async (m, { conn, args, text, usedPrefix, command }) => {
  
  // 1. التحقق الاستباقي: إذا لم يكتب المستخدم الكلمة أو العبارة المراد البحث عن صورتها
  if (!text) throw `✳️ يرجى إدخال نص أو اسم الشيء المراد البحث عن صورته.\n\n📌 *مثال:* *${usedPrefix + command}* طبيعة خلابة`
  
  // 2. استدعاء محرك جلب الصور بناءً على النص المدخل من المستخدم
  let res = await fg.gimage(text)
  
  // 3. اختيار صورة عشوائية من مصفوفة النتائج الناجحة وإرسالها إلى الدردشة
  conn.sendFile(m.chat, res.getRandom(), 'img.png', `✅ تم العثور على النتيجة بجودة عالية`, m, null, fwc)
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها لسهولة الوصول من المنيو العربي
handler.help = ['صورة [نص_البحث]']
handler.tags = ['الوسائط والترفيه']

// التعبير النمطي للمصفوفة البرمجية لدعم الكلمات العربية والإنجليزية بالتبادل
handler.command = /^(img|image|gimage|imagen|صورة|صوره|ابحث_عن_صورة)$/i

// إعدادات الاقتصاد الداخلي للبوت (RPG)
handler.diamond = true // يخصم (1 ألماس) من رصيد المستخدم عند كل عملية بحث ناجحة لتنظيم الاستهلاك

export default handler
