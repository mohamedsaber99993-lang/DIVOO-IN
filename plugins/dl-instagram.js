import fg from 'fg-senna'

let handler = async (m, { conn, text, args, usedPrefix, command }) => {
  
  // 1. التحقق من وجود الرابط: إذا لم يقم المستخدم بوضع الرابط بجانب الأمر، يتم توجيهه للمثال الصحيح
  if (!args[0]) throw `✳️ يرجى إدخال رابط محتوى من إنستغرام (Instagram).\nمثال: ${usedPrefix + command} [رابط_الفيديو]`
  
  // إظهار تفاعل الانتظار (جاري التحميل)
  m.react(rwait)

  try {
    // 2. استدعاء دالة igdl من مكتبة fg-senna لتحليل الرابط واستخراج ميديا التحميل المباشرة
    let res = await fg.igdl(args[0])
    
    // 3. إرسال ملف الميديا الناتج (الفيديو أو الصورة) مباشرة إلى المحادثة مع نص النجاح
    await conn.sendFile(m.chat, res.dl_url, 'ig.mp4', `✅ تم التحميل بنجاح`, m, null, fwc)
    
    // إظهار تفاعل النجاح واكتمال الإرسال
    m.react(done)
    
  } catch (error) {
    // التعامل الآمن مع الأخطاء في حال كان الرابط خاصاً أو تالفاً لحماية البوت من التوقف
    console.error(error)
    m.reply("❌ حدث خطأ أثناء التحميل، يرجى المحاولة مرة أخرى لاحقاً.") 
  }
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها
handler.help = ['تحميل_إنستا'].map(v => v + ' <الرابط>')
handler.tags = ['التحميلات / الميديا']

// مصفوفة الكلمات المفتاحية التي يستجيب لها البوت (تم إضافة الكلمات العربية لتسهيل الاستخدام)
handler.command = ['تحميل_إنستا', 'انستغرام', 'انستا_دلو', 'igdl', 'instagramdl', 'instagram']
handler.diamond = true // استهلاك نقاط الألماس عند تفعيل الأمر (إن كان نظام النقاط مفعلاً)

export default handler
