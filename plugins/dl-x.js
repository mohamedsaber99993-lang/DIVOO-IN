import fg from 'fg-senna'

let handler = async (m, { conn, args, usedPrefix, command }) => {
    
    // 1. التحقق من وجود الرابط: إذا لم يقم المستخدم بوضع الرابط بجانب الأمر، يتم توجيهه للمثال الصحيح
    if (!args[0]) throw `📌 يرجى إدخال رابط التغريدة التي تحتوي على الفيديو من منصة إكس (X / Twitter).\n\n✳️ مثال على الاستخدام :\n*${usedPrefix + command}* https://twitter.com/fernandavasro/status/1569741835555291139`
    
    // إظهار تفاعل الانتظار (جاري التحميل)
    m.react(rwait)
          
    try {
        // 2. استدعاء دالة twitter من مكتبة fg-senna لتحليل الرابط واستخراج البيانات والميديا
        let { SD, HD, desc, thumb, audio } = await fg.twitter(args[0])
        
        // 3. صياغة نص الوصف الخاص بالتغريدة باللغة العربية
        let te = ` 
┌─⊷ *تحميل من منصة X 🎬*
▢ *📝 وصف التغريدة:* ${desc || 'لا يوجد وصف'}
└───────────────────`

        // 4. إرسال ملف الفيديو الناتج بجودته العالية (HD) مباشرة إلى المحادثة مع النص المنسق
        await conn.sendFile(m.chat, HD, 'twitter.mp4', te, m, null, fwc)
        
        // إظهار تفاعل النجاح واكتمال الإرسال
        m.react(done)

    } catch (e) {
        // التعامل الآمن مع الأخطاء في حال كان الحساب خاصاً أو الفيديو محذوفاً
        console.error(e)
        m.reply(`✳️ حدث خطأ أثناء محاولة جلب الفيديو، يرجى التأكد من صحة الرابط وأن الحساب عام وليس خاصاً.`)
    } 
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها لسهولة الوصول من المنيو العربي
handler.help = ['تحميل_تويتر'].map(v => v + ' <الرابط>')
handler.tags = ['التحميلات / الميديا']

// مصفوفة الاختصارات والكلمات المفتاحية للأمر (يدعم اسم المنصة القديم والجديد والعربي)
handler.command = ['تويتر', 'إكس', 'تويت', 'tw', 'twitter', 'x']
handler.diamond = true // استهلاك نقاط الألماس عند تفعيل الأمر

export default handler
