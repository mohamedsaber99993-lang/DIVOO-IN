import fetch from 'node-fetch'
import fg from 'fg-senna' 

// إعداد الحد الأقصى المسموح به لحجم الفيديو بالميجابايت (320 ميجابايت) حمايةً لموارد السيرفر
let limit = 320

let handler = async (m, { conn, args, isPrems, isOwner, usedPrefix, command }) => {
	
    // 1. التحقق من وجود الرابط وإدخاله بجانب الأمر
    if (!args || !args[0]) throw `✳️ يرجى إدخال رابط مقطع فيديو من يوتيوب (YouTube).\n\n📌 مثال على الاستخدام :\n${usedPrefix + command} https://youtu.be/YzkTFFwxtXI`
    
    // 2. التحقق الأولي للتأكد من أن الرابط يخص منصة يوتيوب فعلياً
    if (!args[0].match(/youtu/gi)) throw `❎ عذراً، يرجى التأكد من أن الرابط الذي أدخلته هو رابط يوتيوب صحيح.`
	
    let chat = global.db.data.chats[m.chat]
    
    // إظهار تفاعل الانتظار (جاري معالجة طلب الفيديو)
    m.react(rwait)  
    
    try {
        // 3. قراءة الجودة المطلوبة من المستخدم كعامل ثانٍ (args[1])، وفي حال لم يحدد جودة يتم تعيين 360p افتراضياً
        let q = args[1] || '360p'
	    
        // استدعاء دالة ytv من مكتبة fg-senna وسحب بيانات المقطع بناءً على الجودة المحددة
        let res = await fg.ytv(args[0], q)

        // تفكيك كائن البيانات المستخرج من خادم يوتيوب الوسطي
        let { title, dl_url, thumb, size, sizeB, duration, quality } = res

        // 4. فحص حجم الملف الفعلي بالبايت (sizeB) للتأكد من عدم تجاوزه للحد المسموح به في السيرفر (320 MB)
        let isLimit = limit * 1024 * 1024 < sizeB

        // إظهار شريط تقدم متحرك (Loading Bar) تفاعلي يتحدث على واجهة الواتساب أثناء التحميل والرفع
        await conn.loadingMsg(m.chat, '📥 جاري التحميل والدفق', ` ${isLimit ? `≡ *تحميل مقاطع يوتيوب 🎬*\n\n▢ *⚖️ الحجم الفعلي:* ${size}\n\n▢ _❌ عذراً، حجم المقطع يتجاوز الحد الأقصى المسموح به لك وهو_ *+${limit} ميجابايت*` : '✅ اكتمل تحميل الملف وتجهيزه للإرسال!' }`, ["▬▭▭▭▭▭", "▬▬▭▭▭▭", "▬▬▬▭▭▭", "▬▬▬▬▭▭", "▬▬▬▬▬▭", "▬▬▬▬▬▬"], m)
 
        // 5. إذا كان حجم الفيديو آمناً وضمن الحدود، يتم إرسال الملف مباشرة للمحادثة مع بيانات الجودة والحجم
        if (!isLimit) {
            await conn.sendFile(m.chat, dl_url, title + '.mp4', `
≡ *تحميل من يوتيوب بنجاح 🎬*

*📌 العنوان:* ${title}
*⚖️ الحجم:* ${size}
*🎞️ الجودة المستخدمة:* ${q}
`.trim(), m, false, { asDocument: chat.useDocument })
        }
        
        // إظهار تفاعل النجاح النهائي واكتمال العملية
        m.react(done) 
		
    } catch (e) {
        // التعامل الآمن مع الأخطاء وتجنب توقف السكريبت في حال تلف الرابط أو عدم توفر الجودة المطلوبة
        console.error(e)
        await m.reply(`❎ حدث خطأ غير متوقع أثناء محاولة تحميل المقطع، يرجى المحاولة لاحقاً أو تجربة جودة أقل مثل 360p أو 480p.`)
    } 
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها لسهولة الوصول من المنيو العربي
handler.help = ['يوتيوب_فيديو'].map(v => v + ' <الرابط> <الجودة>')
handler.tags = ['التحميلات / الميديا']

// مصفوفة الكلمات المفتاحية والاختصارات للأمر باللغتين العربية والإنجليزية
handler.command = ['يوتيوب_فيديو', 'فيديو', 'تحميل_فيديو', 'ytmp4', 'fgmp4']
handler.diamond = false // جعل الأمر مجانياً بالكامل ولا يستهلك نقاط الألماس

export default handler
