import { spawn } from 'child_process'

let handler = async (m, { conn, isROwner, text }) => {
    // التحقق من طريقة تشغيل البوت؛ حيث يجب أن يكون قد تم تشغيله عبر ملف index.js ليدعم المراقبة وإعادة التشغيل
    if (!process.send) throw 'خطأ في طريقة التشغيل:\nلا تستخدم: node main.js\nاستخدم: node index.js'
    
    // التحقق من معرف البوت الحالي
    if (conn.user.jid == conn.user.jid) {
    // 1. إرسال إشعار بدء إعادة التشغيل باللغة العربية
    await m.reply(`🔄 جاري إعادة تشغيل وتحديث النظام...`)
    
    // 2. إرسال أمر إعادة التشغيل إلى المعالج الأساسي للسيرفر
    process.send('reset')
  } else throw 'حدث خطأ في تحديد المعرف'
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها لسهولة الوصول من المنيو العربي
handler.help = ['إعادة_التشغيل']
handler.tags = ['أوامر المطور والمشرف']

// مصفوفة الكلمات المفتاحية والاختصارات للأمر باللغتين العربية والإنجليزية
handler.command = ['restart', 'reiniciar', 'ريستارت', 'إعادة_تشغيل', 'اعاده_تشغيل'] 

// قفل أمان مطلق: متاح فقط وحصرياً لمالك ومنشئ البوت الأساسي
handler.rowner = true

export default handler
