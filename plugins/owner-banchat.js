let handler = async (m, { conn, isOwner, isAdmin, isROwner }) => {
    
    // 1. قفل الحماية: التحقق مما إذا كان مرسل الأمر مشرفاً أو مطوراً، وإلا يتم رفض الطلب تلقائياً
    if (!(isAdmin || isOwner)) return dfail('admin', m, conn)
    
    // 2. تحديث قاعدة بيانات الجروب وتحويل حالة الحظر إلى (مفعّلة) لتجميد استجابة البوت
    global.db.data.chats[m.chat].isBanned = true
    
    // 3. إرسال رسالة التأكيد باللغة العربية
    m.reply(`✅ تم تعطيل وإيقاف تشغيل البوت بنجاح في هذه المجموعة.`)
}

// إعدادات المساعدة والتصنيف للأمر بعد تعريبها لسهولة الوصول من المنيو العربي
handler.help = ['تعطيل_البوت']
handler.tags = ['أوامر المطور والمشرف']

// مصفوفة الكلمات المفتاحية والاختصارات للأمر باللغتين العربية والإنجليزية
handler.command = ['تعطيل_البوت', 'اغلاق_البوت', 'إغلاق_البوت', 'banchat', 'chatoff'] 

// قفل الأمان الأساسي: تشغيل الأمر داخل المجموعات فقط لارتباطه بإعدادات الشات
handler.group = true

export default handler
